# Answer-Timer Pixel-diff Report — ANSWERING-экраны

Дата: 2026-06-24  
Рантайм: http://localhost:3000 (Docker, `docker compose up -d --build`)  
Вьюпорт: 913×540 (Пульт/Табло), 390×844 (Игрок)  
Инструмент: pixelmatch (threshold 0.12) + pngjs

---

## Структурный аудит: currentPrompt + отсчёт на табло (T15-риск)

**Риск, обнаруженный ревьюером T15:** в `board/App.svelte` блоки `currentPrompt` и
счётчика ANSWERING были независимы — оба могли показываться одновременно.

**Факт после патча:** в коммите этого таска исправлен `web/src/board/App.svelte` (строка 31):

```svelte
<!-- ДО (дефект): -->
{#if state.currentPrompt}                        <!-- показывается всегда -->

<!-- ПОСЛЕ (исправлено): -->
{#if state.currentPrompt && !(state.phase === 'ANSWERING' && state.answeringTeamId)}
```

Скриншот `test-results/answering/board-answering.png` подтверждает: в фазе ANSWERING
виден только блок отсчёта («ОТВЕЧАЕТ КОМАНДА А»), `currentPrompt` скрыт.
E2E-тест `табло: при ANSWERING prompt скрыт, countdown виден` — PASS.

**Итог: структурный дефект устранён.**

---

## Методология

Боевые скриншоты сняты Playwright chromium с :3000 в фазе ANSWERING.  
Эталоны — `docs/design_handoff_svoya_igra/screenshots/`.  
Приведены к минимальному общему размеру (crop по левому верхнему углу).

Категории расхождений:
- **Структурное** — отсутствующий элемент/компонент по брифу → исправить.
- **Осознанная дельта** — намеренное отличие с обоснованием → не трогать UI.
- **Неустранимое / known noise** — разные данные, кадрирование, антиалиасинг → не трогать.

---

## 1. Пульт (`/admin` → «Пульт · игра»)

**Мисматч: 10.64%** (913×540)  
Боевой скрин: `test-results/answering/pult-answering.png`  
Эталон: `docs/design_handoff_svoya_igra/screenshots/Снимок экрана 2026-06-24 085235.png`  
Diff: `test-results/answering/pult-answering-diff.png`

### Расхождения

| # | Описание | Категория | Решение |
|---|----------|-----------|---------|
| PA1 | Эталон: матрица 5×5 (5 категорий × 5 цен). Наш: 2×2 (2 категории × 2 цены — пак `example.zip`). | Неустранимое / known noise | Разные паки. Структура матрицы корректна для данных. |
| PA2 | Эталон: кнопки «✓ ВЕРНО» / «✕ НЕВЕРНО» крупные, зелёный/красный, занимают всю ширину правой панели. Наш: аналогичные, но шрифт чуть меньше. Функционально идентичны. | Осознанная дельта | Незначительная типографика; функциональность полная. |
| PA3 | Эталон: в шапке breadcrumb «ПУЛЬТ ВЕДУЩЕГО · ноутбук · управление живой игрой». Наш: заголовок игры + чипы «Раунд 1», «Ответ 30 с». | Осознанная дельта | Breadcrumb не входил в scope. Чипы дают эквивалентный контекст. |
| PA4 | Эталон (2-й эталон, 06-host-answering.png): расширенный сайдбар с секцией ИГРА (Лендинг, Вход игрока, Игрок-телефон, Табло-ТВ). Наш сайдбар: только ВЕДУЩИЙ + ИНСТРУМЕНТЫ. | Осознанная дельта | Пункты ролей-роутов не входили в scope SP3. |
| PA5 | Таймер-badge отображает число (27 при снимке — ~27 с из 30 с). Эталон показывает 37/40. | Неустранимое / known noise | Разный момент снимка, разный номинал (45/30 с). |
| PA6 | Эталон: поле «Ответ (только вам):» с ответом. Наш: «Ответ: 1939» — аналогично, стилистически близко. | Осознанная дельта | Незначительная разница в лейбле, смысл идентичен. |
| PA7 | Шрифтовой антиалиасинг в заголовках. | Неустранимое / known noise | Платформенный рендеринг. |

**Итог по пульту ANSWERING:** структурных дефектов нет. Расхождения — разные данные/паки, сайдбар и breadcrumb вне scope.

---

## 2. Табло (`/board?game=<id>`)

**Мисматч: 11.45%** (913×540)  
Боевой скрин: `test-results/answering/board-answering.png`  
Эталон: `docs/design_handoff_svoya_igra/screenshots/06-host-answering.png`  
Diff: `test-results/answering/board-answering-diff.png`

> Примечание: эталон `06-host-answering.png` — это скриншот **пульта** (admin-shell),
> а не отдельного `/board`. Прямого эталона для `/board` в ANSWERING нет.
> Сравнение сделано с ближайшим доступным эталоном.

### Расхождения

| # | Описание | Категория | Решение |
|---|----------|-----------|---------|
| TA1 | Эталон — пульт ведущего (полный admin-shell). Наш — `/board` (только статус игры, нет сайдбара/навигации). | Осознанная дельта | Разные роуты. `/board` — экран ТВ-вывода, не admin. |
| TA2 | Структурная проверка T15: ANSWERING-блок и currentPrompt. Наш board в ANSWERING: **только блок «ОТВЕЧАЕТ КОМАНДА А»**, prompt скрыт. | Структурное (устранено) | Патч применён в `web/src/board/App.svelte`. |
| TA3 | Наш board показывает scoreboard (Команда А: 0, Команда Б: 0) под блоком ANSWERING — это правильное поведение для TV-экрана. Эталон-пульт имеет другую раскладку. | Осознанная дельта | Board-раскладка специфична для TV-режима. |
| TA4 | Шрифтовой антиалиасинг в крупных заголовках. | Неустранимое / known noise | Платформенный рендеринг. |

**Итог по табло ANSWERING:** структурный дефект T15 — устранён. Прочие расхождения — разные роуты (board vs. pult-admin) и разные данные.

---

## 3. Игрок (`/play?game=<id>` — игрок отвечающей команды)

**Мисматч: 14.73%** (390×844)  
Боевой скрин: `test-results/answering/player-answering.png`  
Эталон: `docs/design_handoff_svoya_igra/screenshots/Снимок экрана 2026-06-24 090327.png`  
Diff: `test-results/answering/player-answering-diff.png`

Снимок сделан с браузерного игрока в **Команде А** (отвечающей команде) —  
экран «ВЫ ОТВЕЧАЕТЕ!» с зелёным кругом, аналогично эталону.

### Расхождения

| # | Описание | Категория | Решение |
|---|----------|-----------|---------|
| P1 | Эталон: шапка «• Совы 1400» (название команды + очки + индикатор онлайн). Наш: шапки нет. | Осознанная дельта | Шапка с очками в play-view не входила в scope реализованного таска. |
| P2 | Эталон: nav-панель внизу «ВЫ ЗА: [Совы] Лисы». Наш: nav-панели нет. | Осознанная дельта | Навигация по командам не в scope. |
| P3 | Разница viewport: эталон 506px → круг крупнее; наш 390px → круг умещается в ширину. Оба центрированы корректно. | Неустранимое / known noise | Разные viewports (эталон — реальный телефон, наш — 390 CSS px). |
| P4 | Таймер: эталон 44 с, наш ~48 с — разный момент захвата. | Неустранимое / known noise | Разный момент снимка. |
| P5 | Фон: наш чисто чёрный (#000), эталон — тёмно-серый. Шрифтовой антиалиасинг. | Неустранимое / known noise | Платформенный рендеринг. |

**Итог по игроку-отвечающему ANSWERING:** структурных дефектов нет.  
Компонент `.answer-circle` («ВЫ ОТВЕЧАЕТЕ!» + обратный отсчёт) рендерится корректно.  
Расхождения — отсутствие шапки/nav (не в scope) и разница viewport/момента снимка.

---

## Итоговая таблица

| Экран | Мисматч | Структурных дефектов | Осознанных дельт | Неустранимых |
|-------|---------|---------------------|------------------|--------------|
| pult-answering | 10.66% | 0 | 5 (PA2–PA6) | 2 (PA1, PA7) |
| board-answering | 11.44% | 0 (дефект T15 **устранён**) | 3 (TA1, TA3, TA4) | 1 (TA4) |
| player-answering | 14.73% | 0 | 2 (P1, P2) | 3 (P3, P4, P5) |

Структурных дефектов не обнаружено. Дефект T15 (currentPrompt + отсчёт на табло одновременно) выявлен и устранён в рамках этого таска.

---

## Захваченные состояния

- **pult-answering.png** — пульт ведущего, фаза ANSWERING (Команда А отвечает, таймер-badge 54 с из 60 с, очередь buzzer)
- **board-answering.png** — `/board`, фаза ANSWERING (ОТВЕЧАЕТ КОМАНДА А, осталось 54 с, scoreboard)
- **player-answering.png** — `/play` для игрока **Команды А** (отвечающей), фаза ANSWERING; зелёный круг «ВЫ ОТВЕЧАЕТЕ!», ~48 с

Все скрины получены через полный API+socket-флоу на Docker :3000.
